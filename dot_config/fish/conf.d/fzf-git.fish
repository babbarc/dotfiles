# fzf-git key bindings (ctrl-g b/f/h/l/r/s/t/w plus ctrl-g ctrl-<key>, and
# ctrl-g ? for the binding list). Sourced from the chezmoi-managed external at
# ~/fzf-git.sh (see .chezmoiexternal.toml); `chezmoi update --refresh-externals`
# pulls newer versions of that clone.
source "$HOME/fzf-git.sh/fzf-git.fish"
