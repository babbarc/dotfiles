lazygit/state.yml
# Per-machine env file (copy env.example to ~/.config/dotfiles/env per machine;
# never commit it - it carries machine-specific personal values)
env
# nix build output symlink in the repo root (running `nix build` from the
# repo root creates it; the sibling nix-config repo's setup.sh builds from
# $HOME instead)
result
# Pi Calm runtime state must never be tracked (see tests/pi-calm.test.sh)
/pi/agent/calm

*.bak
.tmux
fzf-git.sh
ohmyzsh
passfzf
